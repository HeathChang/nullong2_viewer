export type { FileKind, FileNode, DirNode, Workspace, WorkspaceMode, LoadedDoc } from './types'
export {
  extensionOf,
  kindOf,
  isStructured,
  isOpenable,
  isImageName,
  isHiddenPath,
  KIND_ORDER,
  KIND_LABEL,
} from './kinds'
